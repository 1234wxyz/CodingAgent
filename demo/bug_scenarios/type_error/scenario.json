{
  "name": "type_error",
  "title": "TypeError: int + str concatenation",
  "description": "format_user_greeting('Alice', 30) raises TypeError because age is an int but the function tries to concatenate it with strings. Fix the function so it works with both int and str age values.",
  "target_paths": ["formatter.py"],
  "verify_command": "python verify.py"
}
